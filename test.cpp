#include <libc.h>
#include <sys/fcntl.h>
#include <unistd.h>
#include <iostream>
#include <ostream>
#include <vector>

int main()
{
	// std::vector<char> vec;
	// vec.resize(10);
	// read(1, vec.data(), 10);
	// std::cout << "first read done\n";
	// vec.resize(16);
	// read(1, vec.data() + 10, 6);
	// for (size_t i = 0; i < vec.size(); i++)
	// 	std::cout << vec[i];
	// std::cout << std::endl;
}
